{
  "name": "blxcode",
  "version": "0.3.3",
  "private": true,
  "description": "Tauri 2 + Leptos desktop app. This root manifest anchors Tauri's frontend-dir resolution so beforeDev/beforeBuild commands run from the repo root.",
  "type": "module",
  "scripts": {
    "tauri:before-dev": "node scripts/tauri-before-build.cjs --dev",
    "tauri:before-build": "node scripts/tauri-before-build.cjs"
  }
}
