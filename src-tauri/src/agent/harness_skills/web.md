# Web

## `web_search` / `web_fetch`

Requires an API key (Settings → Agent → Web Tools, or `BLX_TAVILY_API_KEY` / `BLX_BRAVE_API_KEY`).

When disabled, the `web` core skill shows `disabled_no_key` in the Skills panel.
